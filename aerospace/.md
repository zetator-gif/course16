**cross-sectional area** - area of a slice taken perpendicular to the cylinder's length (flat face)  <br>
to describe continuous change, we take a **very thin horizontal layer** of thickness $d_h$

**infinitesimal** instead of zero allows the step :

$$
p-p≈\frac{dp}{dh}dh
$$

"This is effectively a linear approximation or the first-order Taylor expansion of p(h) around the point h."

## multivariable continuity
**continuously varying f()** - small changes in the input(x) result in small, predictable changes in the output(f(x))

**horizontal planes** - infinite number of perpendicular planes  <br>

## extended real line
$$
R=R∪{+∞,−∞} 
$$

```python
import numpy as np
import matplotlib.pyplot as plt

x = np.array([1, 3, 5, 10, 50, 100, 900, 1000])
y = np.log(x)  # natural log

plt.plot(x, y, marker='o')
plt.xscale('log')           # keeps x-axis as 10, 100, 1000, ...
plt.xlabel("x")
plt.ylabel("ln(x)")
plt.title("y = ln(x)")

plt.grid(True, which="both", ls="--", alpha=0.5)
plt.show()

```
<img width="590" height="450" alt="image" src="https://github.com/user-attachments/assets/d54eada2-ef0e-4b70-90e2-73111db813b2" />
